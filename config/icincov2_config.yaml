common: 
  out_dir: "/media/arvc/data/datasets/icinco/06"
  num_env: 1000
  save_pcd: true
  pcd_binary: true
  paused: false
  debug_msgs: true

collision:
  model: "truss" # model to compute the collisions with the sensor

sensor:
  name: "os_128"
  topic: "/os1/pointCloud"
  collision_offset: 1 # % to increase the collision box of the original sensor

poses:
  offset: 2.0
  rand_mode: uniform # uniform, gaussian
